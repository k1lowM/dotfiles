dotfiles
====

## how to set up.

(Windowsのみ)

    $ git clone https://github.com/k1lowM/dotfiles.git ~/dotfiles
    $ cd ~/dotfiles
    $ ./mklink.bat
    $ git submodule update --init

vimを起動して

    :PluginInstall
